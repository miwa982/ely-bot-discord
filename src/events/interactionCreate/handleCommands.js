/**
 * CommandKit automatically handles and executes all slash commands and context menus.
 * Running commands manually here causes duplicate execution and DiscordAPIError[10062] race conditions.
 */
export default async (interaction, client) => {
  return;
};
